import React from 'react';
import {PageHeader} from 'antd';

const Header = () => {
  return (
      <div className={'shadow-xl p-6 flex items-center'}>
        <div className={'font-semibold'}>
          Effeckts
        </div>
      </div>
  );
};

export default Header;
